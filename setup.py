#!/usr/bin/env python
from setuptools import setup, find_packages

setup(
    name='shapely', # This is the name of your PyPI-package.
    version='1.6b4', # Update the version number for new releases
    packages=find_packages(),
    include_package_data=True
)
